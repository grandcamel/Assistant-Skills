# Assistant Skills Dependencies

# Shared library (replaces skills/shared/scripts/lib/)
assistant-skills-lib>=0.1.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-xdist>=3.0.0
pytest-timeout>=2.0.0

# Output formatting (included in assistant-skills-lib, but listed for clarity)
tabulate>=0.9.0
